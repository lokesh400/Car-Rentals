<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">    
    <title>Edit Car Details</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">    
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://ai-public.creatie.ai/gen_page/tailwind-custom.css" rel="stylesheet">    
    <script src="https://cdn.tailwindcss.com/3.4.5?plugins=forms@0.5.7,typography@0.5.13,aspect-ratio@0.4.2,container-queries@0.1.1"></script>
    <script src="https://ai-public.creatie.ai/gen_page/tailwind-config.min.js" data-color="#F5BB1B" data-border-radius='medium'></script>
    <link rel="icon" type="image/png" href="images/logo.jpg">
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-xl mx-auto p-8">        
      <div class="bg-white rounded-xl shadow-lg p-8">
            <div class="mb-8">
                <h1 class="text-2xl font-semibold text-gray-800">Edit Car Details</h1>
                <p class="text-gray-500 mt-2">Update the information for your vehicle</p>
            </div>            
            <form action="/admin/update-car/<%= car._id %>?_method=PUT" method="POST" class="space-y-6">
                <div class="relative">                    
                  <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Car Name</label>
                    <div class="relative">                        
                      <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                            <i class="fas fa-car"></i>
                        </span>
                        <input type="text" id="name" name="name" value="<%= car.name %>" required 
                            class="block w-full pl-10 pr-3 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-custom focus:border-custom transition-colors bg-white text-gray-900 text-sm placeholder-gray-400"
                            placeholder="Enter car name">                    
                          </div>
                </div>

                <div class="relative">
                    <label for="brand" class="block text-sm font-medium text-gray-700 mb-1">Car Brand</label>
                    <div class="relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">                            
                          <i class="fas fa-building"></i>
                        </span>
                        <input type="text" id="brand" name="brand" value="<%= car.brand %>" required 
                            class="block w-full pl-10 pr-3 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-custom focus:border-custom transition-colors bg-white text-gray-900 text-sm placeholder-gray-400"
                            placeholder="Enter car brand">                    
                          </div>
                </div>

                <div class="grid grid-cols-2 gap-6">
                    <div class="relative">                        
                      <label for="year" class="block text-sm font-medium text-gray-700 mb-1">Manufacturing Year</label>
                        <div class="relative">                            
                          <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">
                                <i class="fas fa-calendar"></i>
                            </span>
                            <input type="number" id="year" name="year" value="<%= car.year %>" required min="1900" max="2024"
                                class="block w-full pl-10 pr-3 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-custom focus:border-custom transition-colors bg-white text-gray-900 text-sm placeholder-gray-400"
                                placeholder="Enter year">                        
                              </div>
                    </div>

                    <div class="relative">
                        <label for="pricePerDay" class="block text-sm font-medium text-gray-700 mb-1">Price Per Day</label>
                        <div class="relative">
                            <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-500">                                
                              <i class="fas fa-dollar-sign"></i>
                            </span>
                            <input type="number" id="pricePerDay" name="pricePerDay" value="<%= car.pricePerDay %>" required min="0"
                                class="block w-full pl-10 pr-3 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-custom focus:border-custom transition-colors bg-white text-gray-900 text-sm placeholder-gray-400"
                                placeholder="Enter price per day">                        
                              </div>
                    </div>
                </div>

                <div class="pt-4">
                    <button type="submit" class="!rounded-button w-full bg-custom text-white py-3 px-4 font-medium hover:bg-custom/90 focus:outline-none focus:ring-2 focus:ring-custom focus:ring-offset-2 transition-colors">                        
                      Update Car Details
                    </button>
                </div>
            </form>
        </div>    
      </div>
</body>
</html>